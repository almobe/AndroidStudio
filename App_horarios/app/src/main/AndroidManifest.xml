<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.usuario.app_horarios">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/Theme.AppCompat.NoActionBar">
        <!--En el manifest empezamos diciéndole que empiece por el splash y despúes pase hacia el main-->
        <!--Le aplicamos el theme creado en styles para que no saque actionBar-->
        <activity android:name=".SplashActivity" android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:theme="@style/Theme.AppCompat.NoActionBar">
        </activity>
        <activity android:name=".Register">
        </activity>
        <activity android:name=".Principal">
        </activity>
        <activity android:name=".Ajustes">
        </activity>
        <activity android:name=".ManagerActivity">
        </activity>
        <activity android:name=".ActivityPorEmpleado">
        </activity>
        <activity android:name=".ActivityAscender"></activity>
    </application>

</manifest>